# Project Notes

- This is the Astro project for `liorabitbol.com`.
- Keep the main professional site at `/` and the writing section under `/blog`.
- The future `blog.liorabitbol.com` setup should be handled at the hosting/DNS layer unless the blog becomes a separate deployment.
- Prefer simple Astro pages/components and plain CSS until the site has enough repeated UI to justify more structure.
- Run `npm run build` before considering a change complete.
